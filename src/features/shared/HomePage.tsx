import { PageFrame } from '../../shared/ui/PageFrame';

export const HomePage = () => {
  return (
    <PageFrame title="Portal Black101" subtitle="Rebuild do portal com foco em segurança, performance e UX fluida.">
      <p>Use o mega menu para navegar entre os módulos de cadastro e administrativo.</p>
    </PageFrame>
  );
};
